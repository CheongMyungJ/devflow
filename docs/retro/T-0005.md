# T-0005: 1단계 전의 스키마 정리(생성 타입의 배열, F12, 고른 작은 것들)와 Store 에 나머지 엔티티

- 유형 / 대상 repo: feature / devflow (시스템 repo 자신 — 스키마, Store, commands, validate-data, 설계 문서)
- Step 수 / 재작업 횟수 / 소요: 4 / 0 (Gate fail 0, 사람의 수정 요청 0, 실패한 실행 0) / 역할 세션의 실행 시간 합계 약 152분(13회: Planner 5회 21분, Worker 4회 82분, Reviewer 4회 49분), 서브에이전트 토큰 합계 약 333만. 벽시계로는 Task 발행 2026-09-19T03:50Z → done 09:15Z, 약 5시간 25분(Intake 의 대화는 그 앞. R-003 은 보고된 실행 16분에 벽시계 2시간 12분 — 원인 모름).
- 결과: done (D-005, AC1~AC13 충족. main 에 직접 merge — 4efebea). Step 한도 5 중 4 사용.
- 수행 주체: Intake 와 Orchestrator 는 AI 대화 세션, Planner·Worker·Reviewer 는 모두 Context 패킷(파일 경로)만 받은 별도 세션. **Orchestrator 대화 세션이 step-002 승인 직후 한 번 바뀌었다.** 모델: Planner·Worker 는 claude-opus-5, Reviewer 는 claude-fable-5-1(사람의 지정 — 대화 세션의 기본 모델이 Opus 로 바뀌어 있어 "기본 그대로" 면 Reviewer 도 Opus 가 될 뻔했다). 이 Task 의 기록은 끝까지 기준 SHA(b11d46d)의 양식·스크립트로 썼고, merge 뒤 main 의 새 스키마로 한 글자도 고치지 않은 채 통과했다(validate-data 624 checked, 0 failed).
- 새 Task 양식(T-0004)으로 발행한 첫 Task 다.

## 성공 기준 원문과의 대조 (done 때 사람에게 보인 표)

| 성공 기준(원문 요지) | 결과 | 남는 한계 |
|---|---|---|
| S1 생성 타입으로 Step(verify)·Task(AC)·Decision(next_step)을 만들어 대입 | 됨 — `tests/generated-types.arrays.test.ts`, role-interface 테스트의 verify 우회 제거, 스키마 검사는 느슨해지지 않음(기준과 76개 값 비교) | verify·next_step 의 합 가지가 `unknown[]`/`{}` 로 생성되어 좁힐 때 보인다(G-002 C). F9 의 조건은 타입에 없다 |
| S2 이벤트마다 commit 식별자, 확인은 식별자로 | 됨 — Store 가 쓰는 모든 이벤트에 `commit_id`, confirmOutcome·createTask 가 식별자로 판정 | 0단계의 운영 스크립트(append-events)가 쓰는 이벤트에는 없다. "재시도에도 같다" 는 만드는 자리로만 보장(실행으로 보일 길이 없다) |
| S3 한 Step 치 기록을 Store 로 쓰고 같은 자리, validate-data 통과 | 됨 — 테스트가 Store 로만 써서 validate-data 0 failed, 배치가 실제 기록과 같다 | 0단계의 실제 기록은 아직 손으로 쓴다(운영 스크립트를 Store 위로 옮기는 것은 범위 밖). AC4 의 "README 의 배치와 같다" 는 merge 뒤 README 를 고쳐 참이 되었다 |
| S4 같은 ID 로 Artifact 버전·Gate 를 다시 쓰면 거부 | 됨 — AlreadyExistsError(Decision·blob 도, 대소문자만 다른 이름도 — step-004) | 사람이 손으로 둔 대소문자 변형 기록 파일을 NTFS 에서 다른 kind 의 get 이 읽는다(사람: "직접 둘 일이 없다"). Windows 밖의 case-folding 파일 시스템은 미확인 |
| S5 기존 기록(T-0001~T-0004)을 고치지 않고 모든 종류로 읽기 | 됨 — `check-store-read` 로 T-0001~T-0005 의 모든 kind invalid 0·읽은 수 = 파일 수 | Windows 11·Node 22.15 에서만 |
| S6 옛 기록과 이 Task 자신의 기록이 새 스키마를 통과 | 됨 — merge 뒤 main 에서 624 checked, 0 failed, 고친 파일 없음 | `human: `(공백만인 id)는 설계대로 통과한다(G-002 C) |
| S7 작은 것 넷(F9, next_step.step 검사, meta 검사, F6) | 됨 | 새 meta 에서도 `stored_in` 은 필수가 아니다(옛 기록 때문) |

## 관찰 1 — 새 양식으로 발행한 첫 Task: 의도의 칸이 값을 했는가

- **발행 전 되짚기가 어긋남을 실제로 잡았다** — A-3 에서 고른 넷을 옮긴 성공 기준이 없었다(AC 는 있어도 끝에서 대조할 원문이 없다) → S7 을 더했다. T-0004 의 "실측되지 않은 경로" 가운데 하나가 처음 확인되었다.
- 그러나 되짚기는 **기존 기록의 실제 쓰임과 대조하지 않아** approved 와 불변의 충돌(meta 를 승인 때 고치는 절차 ↔ Artifact 는 불변)을 놓쳤고 Planner R-001 이 잡았다. 사람이 step-001 승인에서 (A) "승인은 Feedback 과 이벤트로만" 을 골랐다.
- 사람은 1단계 초안을 고치지 않았다(두 Task 연속). 어긋남은 2단계 되짚기와 Planner 가 잡았다.
- planner.md·worker.md 의 새 문장(non_goals 를 scope.exclude 로, 맡겨진 질문을 질문 id 와 함께)을 역할 세션이 따랐다 — D-001~D-004 의 scope.exclude 에 non_goals 가 옮겨졌고 Q2~Q5 가 id 와 함께 남았다. reviewer.md 4번의 새 한 줄은 부딪힐 상황이 없었다(non_goals 를 넘은 곳이 없었다).
- intake.md 다듬기 넷 가운데 (2) 1단계 뒤의 변경을 다시 확인, (4) 선택지와 권고가 다시 필요했다. (1)(3) 은 이번엔 보이지 않았다.

## 관찰 2 — 큰 코드 Task 에서도 출력이 한 번에 받아들여지는가

**그렇다. 13번의 역할 실행 모두 `output_attempts: 1`.** 재작업 0, Gate fail 0. 한 Step 에 AC 다섯(step-003)을 묶어도 재작업 없이 승인되었다.
- **결함은 이번에도 "확인하지 못한 것" 에서 나왔다.** Reviewer R-009 가 Worker 의 "대소문자를 가리지 않는 파일 시스템에서 두 key 가 한 파일이 된다(실행하지 않았다)" 를 실제로 돌려, 불변 기록이 덮어쓰이는 것을 재현했고 Worker 노트에 없던 경우(Artifact 이름)까지 찾았다. Gate 는 pass(B)였다 — class A 가 아니라 B 로 온 실제 결함.
- **새 경로: "승인하되 다음 Step 에서 반드시 고친다".** 사람은 재작업(권고) 대신 v1 을 승인하고 결함을 다음 Step(문서 Step)에 포함시키는 것을 골랐다. Planner R-010 이 F-005 를 goal·done_when 에 옮겼고, step-004 의 Worker 는 "고치기 전 코드에서 실패" 를 두 파일 시스템에서 실행으로 보였다. Worker·Reviewer 한 쌍을 아꼈다. 대가: 승인된 step-003 v1 에 구멍이 남았던 기간, 그리고 이 "반드시" 를 담을 자리가 스키마에 없다(Feedback 의 text 와 Ledger 에만 — backlog).

## 관찰 3 — 모델

- Worker(Opus) 4회 약 82분·약 142만 토큰, Reviewer(Fable) 4회 약 49분·약 111만, Planner(Opus) 5회 약 21분·약 80만. Worker 와 Reviewer 가 다른 모델인 Step 이 넷 모두 성립했다. Reviewer 의 실행 재현(대소문자, 76de40b 로 되돌린 사본)이 두 번 결정적이었다 — 모델 차이의 효과인지 자리의 효과인지는 여전히 가를 수 없다.

## 대화 세션을 도중에 바꿨는데 기록만으로 이어받을 수 있었는가

**있었다.** 새 세션은 절차 문서 → Ledger(세션 인계 절) → events 의 끝 20줄 → task.yaml·D-001·D-002·step-002 의 기록 → roles 의 순서로 읽고, 두 repo 와 worktree 둘의 git status, 끝나지 않은 Run 이 없음을 확인한 뒤 사람에게 상태를 보이고 확인받았다. 인계 절의 "반드시" 목록과 "Reviewer 는 Fable 로 지정" 이 그대로 지켜졌다.
- **인계 뒤에 앞 세션이 commit 을 하나 더 했다**(e0a544b — 사람과의 대화로 후속 후보 하나). 새 세션이 역할 세션을 띄우기 직전에 데이터 repo 의 HEAD 를 다시 보다가 잡았고, 사람에게 확인받았다("이제 앞 세션에선 더 쓰지 않을게"). 두 Orchestrator 가 동시에 쓰면 seq·Ledger 가 겹친다 → 절차 문서에 넣었다.
- 부족했던 것: 앞 세션이 실제로 보낸 패킷 원문이 없었다(→ R-007 부터 `R-NNN.packet.md` 로 남긴다 — 이후 패킷을 다시 짤 필요가 없었다), 인계 절에 데이터 repo 의 push 상태가 없었다, 인계 시각이 "07:2xZ".
- **절차 문서에 없던 단계를 놓쳤다**: 앞 세션은 전이마다 step.yaml 의 status 를 고쳤는데 새 세션은 step-003 에서 defined 에 둔 채 진행했다(이벤트는 모두 남김). 승인 때 알아채 closed 로 맞췄다 → 절차 문서에 넣었다. 기록을 손으로 쓰는 동안은 "이벤트에서 파생되는 필드" 가 어긋날 수 있다 — Store 가 쓰게 되면 사라질 종류의 실수다.

## Planner Step 제안을 사람이 고친 것
- 없음. D-001~D-004 를 모두 그대로 확정했다(`human_edit: false` 넷). T-0002 부터 열 번 연속이다.
- 네 번 모두 Planner 가 "승인된 기록에 없던 것을 새로 정한 것" 을 분리해 올렸고(D-003 여섯, D-004 일곱) 사람은 그것을 보고 확정했다. Orchestrator 는 스키마를 고치는 Step 의 두 검사(생성 타입 사용, 옛 기록의 읽기 전용 checkout)가 verify 에 있는지를 매번 확인해 보고했다 — 빠진 적은 없었다.

## 재작업
- 없음. 사람의 판단이 결과를 바꾼 곳은 둘: step-001 승인 때의 설계 선택 셋(F-001 — approved 충돌은 (A), Decision 불변, blob 을 commit 안에서), step-003 승인 때 "결함을 다음 Step 에 포함"(F-005).

## 실행 중 개입
- 없음. 역할 세션을 기다리는 동안 사람이 설계 질문과 방향 넷을 냈다(Planner 의 역할 범위 / 사람 승인의 기준과 approval 을 정하는 주체·설정, 검증 없는 Step / 검토 요청의 형식 / 작업 기록을 GitHub 이슈로 발행 — 사람이 정한 것 다섯). 모두 backlog 4번으로 옮겼다.

## 반복 패턴 (Skill 후보)
- "스키마를 고친다 + 생성 타입을 실제로 써 보는 검사 + 옛 기록이 고치지 않은 채 통과하는가(읽기 전용 checkout)" — T-0003, T-0004, 이 Task 의 step-002 로 세 번째. verify 의 deterministic 다섯(typecheck, test, validate-data 를 기준 SHA 의 수 + meta 수와 비교, checkout 불변, 생성 타입 미commit)이 step-002~004 에서 같은 모양으로 되풀이되었다.
- "결함을 고친다 + 고치기 전 코드에서 새 테스트가 실패함을 실행으로 보인다" — step-004. 되돌린 사본에서의 재현을 Reviewer 도 같은 방식으로 했다.

## Context 부족 / 과잉
- Ledger 의 Step 요약(무엇이 / 뒤가 알아야 할 것 / 상세의 위치)이 다섯 Planner 모두에게 충분했다("다음 행동의 틀은 Ledger 로 섰다"). 모두 세부를 위해 작업 노트·Gate·코드를 직접 읽었다.
- 반복된 부족: Feedback 을 Step 의 inputs 로 가리킬 수 없다(F-005 를 goal 에 옮겨 적고 패킷에 경로를 넣었다), 실험·재현에 쓸 빌드물의 자리(Worker 는 node_modules/.cache 에, Reviewer 는 테스트의 빌드를 읽었다), 대소문자를 가리는 파일 시스템을 얻는 법, ADR 을 언제 쓰는가의 기준(여섯 번째).
- 패킷의 틀에 더한 것(운영 메모에 적은 것): 패킷 원문을 `R-NNN.packet.md` 로, Worker·Reviewer 패킷에 완료 조건을 더한 Feedback(F-005)의 경로, done 의 Planner 에게 AC 별 검사 명령과 새 checkout.

## Orchestrator(대화 세션)의 실수
- 앞 세션: Ledger 를 문자열 치환 스크립트로 고치다 `$` 가 치환 패턴이 되어 한 줄이 깨진 채 commit(R-004 가 그 Ledger 를 받았다), heredoc 이 꼬여 명령이 반쯤 실행, Run 에 옮긴 packet_gaps 의 `\S` 가 YAML 이스케이프 오류(validate-data 가 잡았다). → Ledger·YAML 은 편집 도구로.
- 새 세션: step.yaml 의 status 를 전이마다 고치지 않음(위), heredoc 을 겹쳐 명령이 문법 오류로 멈춤(아무것도 쓰이지 않음), Run 의 제출 시각을 한 번 추정값으로 적음(commit 전에 고침), merge 명령에 `-F -` 를 써서 실행되지 않음(다시 함), 절차 문서에 annotations 에 대한 틀린 문장을 한 번 씀(바로 고침).
- 역할 세션의 금지 넘기: R-004 가 임시 스크립트를 main checkout 에, R-005 가 타입 프로브 사본을 Workspace 의 tests/ 에 잠깐 두었다 지웠다(스스로 보고). R-002 는 node_modules junction 의 부작용. step-003 부터는 패킷(Planner 의 goal)이 "임시 파일은 OS 의 임시 위치에만, node_modules 를 링크로 잇지 않는다" 를 말했고 넘긴 일이 없었다.

## 후속 후보를 backlog 로 옮긴 것

Ledger 의 "후속 Task 후보" 18건 + 이 회고의 개선 조치. 상세는 `devflow-data/backlog.md` 의 "(T-0005)" 표시.

| 처리 | 건수 | 무엇 |
|---|---|---|
| Task 안에서 또는 merge 직후에 끝남 — 옮기지 않음 | 5 | README 의 배치(merge 뒤 고침), registry.mjs 의 머리 주석(merge 뒤 한 줄), 대소문자 구별 시험 방법·세션 교대의 셋·"실행 중 지시: 없음" 의 패킷 틀(절차 문서에 넣음) |
| 새 항목으로 옮김 | 9 | 읽기 쪽 철자 확인의 한계, validate-data 와 Store 의 Task·Step 디렉터리 규칙, 빌드·복사 코드의 통합과 빌드물의 자리, "승인하되 다음 Step 에서 반드시" 의 자리, 작업 기록을 GitHub 이슈로(사람이 정한 것 다섯), 검토 요청의 형식과 재료, 자동 승인의 조건과 approval 을 정하는 주체·검증 없는 Step, Planner 의 역할 범위, 설계 Step 의 검증한 초안을 노트에 원문으로 |
| 기존 항목에 덧붙임 | 4 | ADR 을 쓸 기준(여섯 번째), inputs 로 가리킬 수 없는 것(Feedback, 이 Task 자신의 기록), Workspace 밖 실험의 의존성, 패킷 원문의 기록(0단계에서는 해결 — Runner 의 몫은 남음) |

## 개선 조치
- [x] docs/stage0-manual-operation.md: 세션 교대(앞 세션은 쓰지 않는다, push 상태), step.yaml 의 status 를 전이마다, 승인하되 다음 Step 에서 반드시, Artifact meta 는 고치지 않는다·쓰는 법(F9), 패킷 원문 `R-NNN.packet.md`, 빌드물의 자리, 대소문자 구별 디렉터리, validate-data 가 검사하는 것과 이름 규칙, check-store-read, done 의 기록 — merge 직후에 했다.
- [x] repo devflow-data 의 README: 실제 배치(기록과 blob, meta 의 F9, commit_id).
- [x] src/schema/registry.mjs 의 머리 주석(G-004 B).
- [ ] 0단계의 기록을 Store 위로 옮길지(운영 스크립트 → commands) — 이 Task 의 non_goal 이었다. backlog 3번의 남은 것.
- [ ] 스키마: Feedback 에 "다음 Step 이 반드시 할 것" 의 자리, Step 의 inputs 로 Feedback 을 가리키는 참조 — backlog 2번.
- [ ] Runner·Orchestrator 를 만들 때: 검토 요청의 형식과 구조화된 재료(decisions_needed, newly_decided, 성공 기준 대조), 작업 기록의 이슈 발행, approval 정책 — backlog 4번.
